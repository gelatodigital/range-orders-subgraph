specVersion: 0.0.2
description: Aave Protection Subgraph
repository: https://github.com/gelatodigital/aave-protection-subgraph
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum/contract
    name: EjectLP
    network: {{network}}
    source:
      address: "{{ejectLP}}"
      abi: EjectLP
      startBlock: {{ejectLPStartBlock}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - RangeOrder
      abis:
        - name: EjectLP
          file: ./abis/EjectLP.json
      eventHandlers:
        - event: LogSetEject(indexed uint256,(uint256,int24,bool,bool,uint256,uint256,address,address,address,uint256),uint256,address)
          handler: handleSetEjectLP
        - event: LogEject(indexed uint256,uint256,uint256,uint256)
          handler: handleEjectLP
        - event: LogCancelEject(indexed uint256)
          handler: handleCancel
      file: ./src/mappings/ejectLPMapping.ts
  - kind: ethereum/contract
    name: RangeOrder
    network: {{network}}
    source:
      address: "{{rangeOrder}}"
      abi: RangeOrder
      startBlock: {{ejectLPStartBlock}}
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.4
      language: wasm/assemblyscript
      entities:
        - RangeOrder
      abis:
        - name: RangeOrder
          file: ./abis/RangeOrder.json
      eventHandlers:
        - event: LogSetRangeOrder(indexed uint256,address,uint256)
          handler: handleSetRangeOrder
      file: ./src/mappings/rangeOrderMapping.ts
